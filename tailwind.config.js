/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./*.{html,js}"], // Tells Tailwind to look at all HTML and JS files in this folder
  theme: {
    extend: {},
  },
  plugins: [],
}
